generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model places {
  id       Int     @id @default(autoincrement())
  name     String  @unique
  category String
  rating   String?
}
